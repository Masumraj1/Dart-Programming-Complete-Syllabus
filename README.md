# 📘 Dart Programming Language – Complete Syllabus (Without OOP)

This syllabus is designed as a **clear, structured, and professional roadmap** for learning **Dart from scratch**, excluding Object-Oriented Programming (OOP). It is suitable for **beginners**, **Flutter learners**, and **hands-on training programs**.

---

## 1️⃣ Introduction to Flutter & Dart

* What is Flutter?
* Why should we learn Flutter?
* What is a Programming Language?
* What is Dart?
* What are Editor Tools?

---

## 2️⃣ Development Environment & Tools

* What is DartPad?
* What is Android Studio?
* What is Git?
* What is GitHub?
* What is Flutter SDK?
* What is JDK?
* What is Gradle / Gradlew?
* What is `pubspec.yaml`?

---

## 3️⃣ Flutter Project Basics

* How to create a Flutter project?
* How to check:

  * Flutter version
  * Dart version
  * Java version
* What is the `main()` function?
* How to run the first Flutter app?
* What is an Emulator?
* What is a Real Device?
* Flutter setup process on Windows

---

## 4️⃣ Variables & Keywords

* What is `var`?
* What is a Dart variable?
* What is `late` variable?
* What is `final` keyword?
* What is `const` keyword?
* Difference between `final` and `const`
* What are Compile-time constants?
* What are Runtime constants?
* What is `static` keyword?

---

## 5️⃣ Operators

* What are Operators?
* Types of Operators in Dart

### Arithmetic Operators

* `+`, `-`, `*`, `/`, `~/`, `%`

### Equality & Relational Operators

* `==`, `!=`, `>`, `<`, `>=`, `<=`

### Type Test Operators

* `is`, `is!`, `as`

### Assignment Operators

* `=`, `*=`, `%=`, `>>>=`, `^=`, `+=`, `/=`, `<<=`, `&=`, `|=`, `-=`, `~/=`, `>>=`

### Logical Operators

* `!`, `||`, `&&`

### Bitwise & Shift Operators

* `&`, `|`, `^`, `~`, `<<`, `>>`, `>>>`

### Conditional Expressions

* `? :`, `??`

### Cascade Notation

* `..`, `?..`

### Spread Operators

* `...`, `...?`

### Other Operators

* `()`, `[]`, `?[]`, `.`, `?.`, `!`

---

## 6️⃣ Comments

* What is a Comment?
* Types of Comments:

  * Single-line comment
  * Multi-line comment
  * Documentation comment

---

## 7️⃣ Control Flow Statements

* What are Control Flow Statements?

---

## 8️⃣ Data Types

* Numbers (`int`, `double`)
* Strings (`String`)
* Booleans (`bool`)
* Records (`(value1, value2)`)
* Functions (`Function`)
* Lists (`List`)
* Sets (`Set`)
* Maps (`Map`)
* Runes
* Symbols
* What is `enum` keyword?
* What is `Future`?
* What is `void`?
* What is `Iterable`?
* What is `Never`?
* What is `dynamic`?
* What is `Object`?
* What is Return Type?
* What are Typedefs?
* What is Type Safety?

---

## 9️⃣ Collections

* What is a Collection?

### List Operations

* `length`
* `add()`
* `addAll()`
* `remove()`
* `removeAt()`

### Advanced Concepts

* What are Generics?
* Why use Generics?
* What is Casting?
* What is Null-assert (`!`)?
* What is Wildcard?
* What are Pattern Types?

---

## 🔟 Control Flow (Detailed)

* What are Loops?
* Types of Loops:

  * `for` loop
  * `while` loop
  * `do-while` loop
* `break` and `continue`
* What is `if-else`?
* What is `switch-case`?

---

## 1️⃣1️⃣ Error Handling & Exceptions

* What is Error Handling?
* What is an Exception?
* What is `throw`?
* What is `try-catch`?
* How does `finally` work?
* What is `assert`?

---

## 1️⃣2️⃣ Functions

* What is a Function?
* What are Parameters?
* Types of Parameters:

  * Named Parameters
  * Optional Positional Parameters
* Types of Functions:

  * Anonymous Functions
* What are Annotations?
* What are Libraries & Imports?

---

## 1️⃣3️⃣ Concurrency & Asynchronous Programming

* What is Synchronous Programming?
* What is Asynchronous Programming?
* What is `async` keyword?
* What is `await` keyword?
* Why do we need `async` and `await`?
* What is `async*`?
* Difference between `async` and `await`
* What are Isolates?

---

## 1️⃣4️⃣ Null Safety

* What is Null Safety?
* What is Sound Null Safety?
* What is Unsound Null Safety?

---

## 1️⃣5️⃣ JSON Handling

* What is JSON?
* What is Encoding?
* What is Decoding?

---

📌 **Note:**

* This syllabus intentionally excludes **Object-Oriented Programming (OOP)**.
* Designed for **hands-on learning**, **Flutter readiness**, and **real-world Dart usage**.
* Suitable for README files, course outlines, and training documentation.
